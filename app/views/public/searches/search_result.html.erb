<table class = "table table-hover table-inverse">
  <h2>検索結果</h2>
  <% unless @books.blank? %>
    <tbody>
      <% @books.each do |book| %>
        <tr>
          <td>
            <%= link_to book_path(book), method: :get do%>
      　  　    <%= image_tag book.item_image_url, size: "100x140" %><br>
        　  　  <%= book.title %><br>
          　　  <%= book.author %>
        　　  <% end %>
        　</td>
        </tr>
      <% end %>
    </tbody>
  <% else %>
    <b>検索された本はまだ登録されていません。</b><br>
    <b>こちらから検索して、本を登録してください。</b>
    <%= render "public/api_searches/api_search"%>
  <% end %>
</table>